import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speechbiofeedback.app',
  appName: 'Speech Biofeedback',
  webDir: 'dist'
};

export default config;
